// Vite ESM minimal fixture — single module with export
// To be evaled with eval_module() (v8::Module API)
export const hello = "Hello from Vite ESM";
export function greet(name) { return "Hi " + name; }
