//! Generated FunctionTemplate stubs — auto-generated by iv8-surface-codegen.

pub mod chrome_extensions;
pub mod crypto;
pub mod css_om;
pub mod dom_core;
pub mod events;
pub mod fetch;
pub mod media_apis;
pub mod observers;
pub mod web_apis;
pub mod web_audio;
pub mod webgl;
pub mod workers;

use v8::Local;
use v8::Object;

/// Install all generated stubs in topological order.
pub fn install_all(scope: &v8::PinScope<'_, '_>, global: Local<Object>) {
    chrome_extensions::install_all(scope, global);
    crypto::install_all(scope, global);
    css_om::install_all(scope, global);
    dom_core::install_all(scope, global);
    events::install_all(scope, global);
    fetch::install_all(scope, global);
    media_apis::install_all(scope, global);
    observers::install_all(scope, global);
    web_apis::install_all(scope, global);
    web_audio::install_all(scope, global);
    webgl::install_all(scope, global);
    workers::install_all(scope, global);
}
